apiVersion: v2
name: kuma
description: Simplified parent chart demonstrating per-mesh zone proxy deployment
type: application
version: 0.1.0

dependencies:
  - name: mesh
    version: "0.1.0"
    repository: "file://../charts/mesh"
